export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Alex's habit tracker",
  description: "A way for Alex to not get fat.",
  navItems: [
    {
      label: "Home",
      href: "/",
    },
  ],
  navMenuItems: [
  ],
  links: {
  },
};
